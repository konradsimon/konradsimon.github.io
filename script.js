<script src="https://cdn.jsdelivr.net/npm/p5@1.3.1/lib/p5.js"></script> <script> //Variables for data, graphics and interface let data = []; let margin = 80; let canvas; let xPos; let yPos; let radius = 20; let isSelected = false; // Load data function preload() { data = loadJSON('https://cdn.freecodecamp.org/testable-projects-fcc/data/choropleth_map/counties.json'); } // Set up canvas and data variables function setup() { canvas = createCanvas(windowWidth, windowHeight); for (let i = 0; i < data.features.length; i++) { let item = data.features[i].properties; data[i] = item; } } // Draw map and interface function draw() { background(200); let maxCountyValue = max(data.map(d => d.total_population)); for (let i = 0; i < data.length; i++) { let x = map(data[i].X, 0, width, margin, width - margin); let y = map(data[i].Y, 0, height, margin, height - margin); let population = data[i].total_population; let colorValue = map(population, 0, maxCountyValue, 0, 255); fill(colorValue); noStroke(); ellipse(x, y, radius); fill(0); textAlign(CENTER, CENTER); textSize(10); text(population, x, y); ellipseMode(CENTER); noFill(); } } // Handle window resizing function windowResized() { resizeCanvas(windowWidth, windowHeight); } // Check for selected circle function mouse


